# Refactor to TypeScript

Convert the provided JavaScript module to TypeScript. Add proper type annotations
for all parameters, return values, and internal variables.

## Requirements

- Rename `math.js` to `math.ts`
- Add TypeScript type annotations to all function signatures
- The file should compile with `tsc --noEmit` (strict mode enabled)
- Keep all existing functionality intact

## Setup

A single `math.js` file exists in the sandbox root with basic arithmetic functions.
